import "server-only";import type { Pool,PoolClient } from "pg";import { AuthorizationError } from "@/lib/access/service";import type { AccessContext } from "@/lib/access/types";import { writeAuditLog } from "@/lib/audit/service";import { dbPool } from "@/lib/core/db/pool";import { withTransaction } from "@/lib/core/db/transaction";import type { RequestContext } from "@/lib/core/http/context";import { ConflictError,NotFoundError } from "@/lib/core/http/errors";import type { ProductCategoryDto,ProductCategoryInput,ProductCategoryUpdateInput } from "@/lib/product-categories/types";
type Ex=Pick<Pool|PoolClient,"query">;type Row={id:string;organization_id:string;parent_id:string|null;code:string;name:string;description:string|null;is_active:boolean;version:number;created_at:Date;updated_at:Date};const map=(r:Row):ProductCategoryDto=>({id:r.id,organizationId:r.organization_id,parentId:r.parent_id,code:r.code,name:r.name,description:r.description,isActive:r.is_active,version:r.version,createdAt:r.created_at.toISOString(),updatedAt:r.updated_at.toISOString()});
function allow(c:AccessContext,p:string){if(!c.permissions.includes(p))throw new AuthorizationError();}function dup(e:unknown):never{if((e as{code?:string}).code==="23505")throw new ConflictError("CONFLICT","The category code already exists.");throw e;}
async function parent(x:PoolClient,c:AccessContext,parentId:string|null,selfId?:string){if(!parentId)return;if(parentId===selfId)throw new ConflictError("CONFLICT","A category cannot be its own parent.");const p=await x.query(`SELECT 1 FROM public.product_categories WHERE id=$1 AND organization_id=$2 AND is_active`,[parentId,c.organization.id]);if(!p.rowCount)throw new NotFoundError("Parent category");if(selfId){const d=await x.query(`WITH RECURSIVE d AS(SELECT id FROM public.product_categories WHERE parent_id=$1 UNION ALL SELECT p.id FROM public.product_categories p JOIN d ON p.parent_id=d.id)SELECT 1 FROM d WHERE id=$2`,[selfId,parentId]);if(d.rowCount)throw new ConflictError("CONFLICT","A category cannot be moved under its descendant.");}}
export async function listProductCategories(c:AccessContext){allow(c,"admin.products.read");return(await dbPool.query<Row>(`SELECT * FROM public.product_categories WHERE organization_id=$1 ORDER BY code`,[c.organization.id])).rows.map(map);}
export async function getProductCategory(c:AccessContext,id:string,x:Ex=dbPool){allow(c,"admin.products.read");const r=await x.query<Row>(`SELECT * FROM public.product_categories WHERE id=$1 AND organization_id=$2`,[id,c.organization.id]);if(!r.rows[0])throw new NotFoundError("Product category");return map(r.rows[0]);}
export async function createProductCategory(c:AccessContext,rc:RequestContext,i:ProductCategoryInput){allow(c,"admin.products.manage");try{return await withTransaction(async x=>{await parent(x,c,i.parentId);const dto=map((await x.query<Row>(`INSERT INTO public.product_categories(organization_id,parent_id,code,name,description,is_active,created_by,updated_by)VALUES($1,$2,$3,$4,$5,$6,$7,$7)RETURNING *`,[c.organization.id,i.parentId,i.code,i.name,i.description,i.isActive,c.user.id])).rows[0]);await writeAuditLog(x,{organizationId:c.organization.id,requestId:rc.requestId,actorUserId:c.user.id,action:"category.created",entityType:"product_category",entityId:dto.id,newData:dto,ipAddress:rc.ipAddress,userAgent:rc.userAgent});return dto;});}catch(e){return dup(e);}}
export async function updateProductCategory(c:AccessContext,rc:RequestContext,id:string,i:ProductCategoryUpdateInput){allow(c,"admin.products.manage");try{return await withTransaction(async x=>{const old=await getProductCategory(c,id,x),m={...old,...i};await parent(x,c,m.parentId,id);const r=await x.query<Row>(`UPDATE public.product_categories SET parent_id=$3,code=$4,name=$5,description=$6,is_active=$7,version=version+1,updated_by=$8 WHERE id=$1 AND organization_id=$2 AND version=$9 RETURNING *`,[id,c.organization.id,m.parentId,m.code,m.name,m.description,m.isActive,c.user.id,i.version]);if(!r.rows[0])throw new ConflictError("VERSION_CONFLICT","The category was updated by another request.");const dto=map(r.rows[0]);await writeAuditLog(x,{organizationId:c.organization.id,requestId:rc.requestId,actorUserId:c.user.id,action:"category.updated",entityType:"product_category",entityId:id,oldData:old,newData:dto,ipAddress:rc.ipAddress,userAgent:rc.userAgent});return dto;});}catch(e){return dup(e);}}
