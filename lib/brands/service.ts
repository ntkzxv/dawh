import "server-only";import type { Pool,PoolClient } from "pg";import { AuthorizationError } from "@/lib/access/service";import type { AccessContext } from "@/lib/access/types";import { writeAuditLog } from "@/lib/audit/service";import { dbPool } from "@/lib/core/db/pool";import { withTransaction } from "@/lib/core/db/transaction";import type { RequestContext } from "@/lib/core/http/context";import { ConflictError,NotFoundError } from "@/lib/core/http/errors";import type { BrandDto,BrandInput,BrandUpdateInput } from "@/lib/brands/types";
type Ex=Pick<Pool|PoolClient,"query">;type Row={id:string;organization_id:string;code:string;name:string;is_active:boolean;version:number;created_at:Date;updated_at:Date};const map=(r:Row):BrandDto=>({id:r.id,organizationId:r.organization_id,code:r.code,name:r.name,isActive:r.is_active,version:r.version,createdAt:r.created_at.toISOString(),updatedAt:r.updated_at.toISOString()});
function allow(c:AccessContext,p:string){if(!c.permissions.includes(p))throw new AuthorizationError();}function dup(e:unknown):never{if((e as{code?:string}).code==="23505")throw new ConflictError("CONFLICT","The brand code already exists.");throw e;}
export async function listBrands(c:AccessContext){allow(c,"admin.products.read");return(await dbPool.query<Row>(`SELECT * FROM public.brands WHERE organization_id=$1 ORDER BY code`,[c.organization.id])).rows.map(map);}
export async function getBrand(c:AccessContext,id:string,x:Ex=dbPool){allow(c,"admin.products.read");const r=await x.query<Row>(`SELECT * FROM public.brands WHERE id=$1 AND organization_id=$2`,[id,c.organization.id]);if(!r.rows[0])throw new NotFoundError("Brand");return map(r.rows[0]);}
export async function createBrand(c:AccessContext,rc:RequestContext,i:BrandInput){allow(c,"admin.products.manage");try{return await withTransaction(async x=>{const dto=map((await x.query<Row>(`INSERT INTO public.brands(organization_id,code,name,is_active,created_by,updated_by)VALUES($1,$2,$3,$4,$5,$5)RETURNING *`,[c.organization.id,i.code,i.name,i.isActive,c.user.id])).rows[0]);await writeAuditLog(x,{organizationId:c.organization.id,requestId:rc.requestId,actorUserId:c.user.id,action:"brand.created",entityType:"brand",entityId:dto.id,newData:dto,ipAddress:rc.ipAddress,userAgent:rc.userAgent});return dto;});}catch(e){return dup(e);}}
export async function updateBrand(c:AccessContext,rc:RequestContext,id:string,i:BrandUpdateInput){allow(c,"admin.products.manage");try{return await withTransaction(async x=>{const old=await getBrand(c,id,x),m={...old,...i};const r=await x.query<Row>(`UPDATE public.brands SET code=$3,name=$4,is_active=$5,version=version+1,updated_by=$6 WHERE id=$1 AND organization_id=$2 AND version=$7 RETURNING *`,[id,c.organization.id,m.code,m.name,m.isActive,c.user.id,i.version]);if(!r.rows[0])throw new ConflictError("VERSION_CONFLICT","The brand was updated by another request.");const dto=map(r.rows[0]);await writeAuditLog(x,{organizationId:c.organization.id,requestId:rc.requestId,actorUserId:c.user.id,action:"brand.updated",entityType:"brand",entityId:id,oldData:old,newData:dto,ipAddress:rc.ipAddress,userAgent:rc.userAgent});return dto;});}catch(e){return dup(e);}}
